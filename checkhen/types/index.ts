export type SimpleUser = {
  email: string;
  name: string;
  handRaiseCount: number;
  overallHandRaiseCount: number;
};

export type StudentStatus = {
  checkedIn: boolean;
  handRaised: boolean;
};